<%= form_for @teacher do |f| %>
    <%= f.label :course  %><br>
    <%= f.collection_select :course_id, @courses, :id, :name %><br>

    <%= f.label :teacher %><br>
    <%= f.collection_select :user_id, @teachers, :id, :name %><br>

    <%= f.submit "Assign" %>
<% end %>

  