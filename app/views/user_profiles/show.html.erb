<div class = 'page-main'>
    <h1>User: <%= @user_profile.full_name %></h1>
    <% if current_user.isAdmin? %>

        
    <% end %>

    <div class="user_info">
        <table style="table-layout: fixed;">
            <tr>
                <td><h4>First Name</h4></td>
                <td>:</td>
                <td><h4><i><%= @user_profile.f_name %></i></h4></td>
            </tr>
            <tr>
                <td><h4>Middle Name</h4></td>
                <td>:</td>
                <td><h4><i><%= @user_profile.m_name %></i></h4></td>
            </tr>
            <tr>
                <td><h4>Last Name</h4></td>
                <td>:</td>
                <td><h4><i><%= @user_profile.l_name %></i></h4></td>
            </tr>
            <tr>
                <td><h4>Email Address</h4></td>
                <td>:</td>
                <td><h4><i><%= @user_profile.user.email %></i></h4></td>
            </tr>
            <tr>
                <td><h4>Phone Number</h4></td>
                <td>:</td>
                <td><h4><i><%= @user_profile.phone_number %></i></h4></td>
            </tr>
        </table>
        <%= button_to :Assign_Program, new_student_grad_path, method: :get, params: {student_id: @user_profile.user.id}, disabled: @user_profile.status != "Pending" unless !current_user.isAdmin? %> 
    </div>
    <% if @user_profile.user.type == :teacher %>
    <div class="teachers_info">
    <h2><u>Teacher specific info</u></h2>
        <h2>Teaching Courses:</h2>
        <ul class = 'w3-container'>
            <% @user_profile.user.teaching_courses.each do |course| %>
                <li>
                    <b>Course: <%= link_to course.name, course_path(course) %></b>
                    <ol>
                        <% course.lessons.each do |lesson| %>
                        <li> Lesson: <%= link_to lesson.title, lesson_path(lesson) %></li>   
                        <% end %>
                    </ol>
                </li>

            <% end %>
        </ul>
    <% end %>
    <% if @user_profile.user.type == :student || @user_profile.user.type == :unassigned %>
    <div class="students_info">
        <h2><u>Student specific info</u></h2>
        <h3>Status: <i><%= @user_profile.status %></i></h3>
        <% if @user_profile.status != "Pending" %>
            #dashboard info about grad_path and different courses/tests
            <h3>Program: <i><%= @user_profile.user.enrolled_program.title %></i></h3>
        <% end %>
    </div>
    <% end %>

</div>

